# Composed Animation Recipes

## Premium Card
`fade-up` + `card-lift` + `card-spotlight-hover`

## Dashboard KPI
`fade-up` (for the wrapper) + `kpi-number-roll` (for the inner value)

## Interactive Button
`fade-up` + `button-glow-soft` + `ripple`
