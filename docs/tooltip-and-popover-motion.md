# Tooltip and Popover Motion (v3.24.0)

`AnimX.tooltip()` provides zero-dependency Popper-like interactions tracking pointer and focus bounds safely.

```javascript
AnimX.tooltip('.help-icon', { effect: 'tooltip-pop' });
```
