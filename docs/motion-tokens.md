# Motion Tokens

Motion Tokens are unified global settings dictating speed, distance, and stagger rates for your animations. 
Tokens allow a simple `text-mask-up` preset to behave wildly differently depending on whether the `durationNormal` token is set to `200ms` or `800ms`.
