# Suggestion Rules

The current heuristic rules are:
- `h1` in a `Hero` section -> `text-mask-up`
- `.card` in a `Features` section -> `fade-up` with stagger
- `button` in a `Hero` section -> `button-glow`
