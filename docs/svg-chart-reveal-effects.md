# SVG Chart Reveals (v3.26.0)

`AnimX.svgChart()` provides motion helpers to animate existing SVG charts. **AnimX is not a data-viz generator**. You must provide the SVG. AnimX simply scales, fades, or draws the existing nodes.
