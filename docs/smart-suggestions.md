# Smart Suggestions

When you scan your imported HTML, AnimX runs a deterministic, rule-based engine to suggest animations.
For example, if it detects multiple `.card` items inside a `<section>`, it will suggest a staggered `fade-up`.

You can preview these suggestions and choose to selectively apply them to the imported HTML.
