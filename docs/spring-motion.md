# Spring Motion (v3.19.0)

Using the `AnimX.spring()` API, you can define physics-driven animations configured via stiffness, damping, and mass. AnimX uses a safe linear approximation mapping Hooke's Law to DOM transforms.
