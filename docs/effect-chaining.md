# Effect Chaining

Effect chaining is the process of linking multiple independent preset behaviors in sequence or in parallel on a single element.

The Composer Engine automatically resolves overlaps and ensures that teardown functions cleanly scrub all associated event listeners.
