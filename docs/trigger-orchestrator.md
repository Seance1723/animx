# Trigger Orchestrator (v3.16.0)

Triggers orchestrate precisely *when* states should shift. Instead of manually binding `element.addEventListener('click', () => AnimX.compose(...))`, the Trigger Orchestrator handles mapping DOM and Custom events directly into AnimX commands safely.
