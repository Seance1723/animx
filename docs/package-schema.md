# Package Schema

The `animx-package` JSON schema defines the structure for all exports:

```json
{
  "schema": "animx-package",
  "schemaVersion": "1.0",
  "animxVersion": "3.4.0",
  "name": "My Project",
  ...
}
```
If you manually edit this file, the Studio Validator will ensure schema integrity before importing.
