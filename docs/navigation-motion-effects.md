# Navigation Motion Effects (v3.22.0)

`AnimX.nav()` combines staggering, active indicators, and scroll-aware reveals into a single orchestrator.

```javascript
AnimX.nav('.site-nav', { 
  item: '.nav-item',
  activeIndicator: 'nav-indicator-slide' 
});
```
