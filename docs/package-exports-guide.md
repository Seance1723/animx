# Package Exports Guide

AnimX uses the modern `package.json` `"exports"` block:
- `animx` defaults to `dist/animx.esm.js` for imports and `dist/animx.min.js` for script tags.
- `animx/css` defaults to `dist/animx.min.css`.
- `animx/core` defaults to `dist/animx.core.esm.js`.
