# UI Feedback Motion States (v3.24.0)

Extending beyond inputs, `AnimX.uiFeedback()` can trigger general visual states like `feedback-success-pop`, `feedback-loading-shimmer`, or `status-badge-pop` across any component.
