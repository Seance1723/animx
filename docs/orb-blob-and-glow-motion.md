# Orbs and Blob Motion (v3.25.0)

`AnimX.orbs()` generates floating DOM nodes that leverage CSS `border-radius` morphing to create liquid-like shapes floating gently in the background.

```javascript
AnimX.orbs('.blob-layer', { count: 3, effect: 'bg-blob-morph' });
```
