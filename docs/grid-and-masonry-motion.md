# Grid and Masonry Motion (v3.23.0)

`AnimX.grid()` routes layouts into the AnimX staggering engine while preserving existing CSS Grid or Flexbox logic.

```javascript
AnimX.grid('.bento-grid', { effect: 'grid-stagger-fade-up' });
```
