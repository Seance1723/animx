# State Variants (v3.16.0)

State Variants bind tightly to the v3.15.0 Composer Variants. Instead of redefining hover arrays, you assign pre-built variants directly into the `AnimX.state()` declaration.
