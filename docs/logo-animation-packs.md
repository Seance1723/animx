# Logo Animation Packs (v3.26.0)

`AnimX.logo()` is specialized for brand logos, allowing distinct sequences for wordmarks and symbols.

```javascript
AnimX.logo('.brand-logo', { effect: 'logo-build-draw-fill' });
```
