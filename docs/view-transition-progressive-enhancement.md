# View Transition API Progressive Enhancement (v3.27.0)

AnimX integrates with the browser's native `document.startViewTransition()` API. If a user is on an older browser that lacks support, AnimX automatically falls back to its CSS animation engine seamlessly.
